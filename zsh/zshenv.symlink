export LD_LIBRARY_PATH=/usr/lib/jni
#export http_proxy=http://localhost:8123
#export https_proxy=http://localhost:8123
export CPATH=/usr/local/uppaal/include
export XDG_DATA_HOME=~/.local/share
path=(~/bin $path /sbin /usr/sbin /usr/local/sbin /opt/flex/bin) 
eval "$(lesspipe.sh)"
LESS=(-r)
export PAGER='less'
# CTAGS Sorting in VIM/Emacs is better behaved with this in place
export LC_COLLATE=C 
export MYZSH=$(dirname $(readlink -f $HOME/.zshrc))
source $MYZSH/lib/functions.zsh
