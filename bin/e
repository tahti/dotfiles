#!/bin/zsh
if [ -z $DISPLAY ] ; then
OPT="-t -s edit -a \"\""
else
OPT="-c -s edit -a \"\""
fi
#${=VAR} - do the word splitting on variable (not needed in bash)
emacsclient ${=OPT} "$@" &! 2>/dev/null 
