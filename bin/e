#!/bin/bash
if [ -z $DISPLAY ] ; then
OPT="-t"
else
OPT="-c"
fi
if [ -z $1 ] ; then
  emacsclient emacs $OPT 2>/dev/null || (
  (emacs --daemon)
  sleep 2
  emacsclient emacs $OPT)
else
  emacsclient emacs $OPT "$*" 2>/dev/null || (
  (emacs --daemon)
  sleep 2
  emacsclient emacs $OPT "$*")
fi
