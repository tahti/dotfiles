#script that should - depending on options:
# -add files in current directory to igrore (possibly recursively/ possibly with adding directories)
# -add extensions in current directory to igrone (possibly recursively)

